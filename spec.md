# massaman-board 仕様書

この文書は massaman-board の機能・画面・動作の「正」となる仕様です。
実装（`index.html`）を変更するときは、先にこの文書を更新し、受け入れ条件に沿って確認します。

- 文書バージョン: 1.0
- 対象実装: 回答者人数の設定（1〜6人）、1列の行数＝回答者数×2 を含む版

---

## 1. 目的と利用場面

マッサマンゲームの進行中に、お題と回答者の発言をリアルタイムで大きく表示する。

- 利用者（操作者）: 司会・入力係。キーボードで発言を入力する
- 閲覧者: 会場の参加者・観客。プロジェクターや大型モニターで表示を見る
- 想定環境: PC のデスクトップブラウザ（Chrome / Edge / Safari / Firefox の最新版）。日本語入力（IME）を使う

## 2. 用語

| 用語 | 意味 |
| --- | --- |
| お題 | ラウンドごとに設定するテーマ文字列 |
| 回答者 | 発言する参加者。1ラウンドにつき1〜6人 |
| 発言 | 回答者が言った単語・フレーズ。入力係がタイプして追加する |
| 発言者 | 次に追加される発言の持ち主として選ばれている回答者 |
| ラウンド | お題の設定から「新しいお題」までの1回分 |

## 3. 画面構成と遷移

```
[お題入力画面] --Enter--> [開始アナウンス] --自動--> [本番画面]
      ^                                              |
      +------------------「新しいお題」----------------+
```

### 3.1 お題入力画面（初期画面）

| ID | 要件 |
| --- | --- |
| S-1 | 見出し「お題を入力してください」を表示する |
| S-2 | お題の入力欄を表示し、画面を開いた時点でフォーカスする |
| S-3 | 回答者の人数を −／＋ ボタンで 1〜6 人の範囲で設定できる。初期値は 3 人。下限・上限ではそれぞれのボタンを無効にする |
| S-4 | 人数分の名前入力欄を、回答者の色の丸印つきで表示する。初期値は「回答者1」「回答者2」… |
| S-5 | 人数を減らしてから増やしても、一度入力した名前は保持する |
| S-6 | お題欄または名前欄で `Enter` を押すと開始する（IME 変換確定の `Enter` は除く） |
| S-7 | お題が空のまま開始しようとした場合は開始せず、お題欄にフォーカスして「お題が空です。入力してください」と表示する |
| S-8 | 名前が空欄の回答者は「回答者N」として扱う |
| S-9 | 右上に「全画面」ボタンを表示する（4.6 参照） |

### 3.2 開始アナウンス

| ID | 要件 |
| --- | --- |
| A-1 | 「それでは開始します」を画面全体に大きく表示する |
| A-2 | 1.6 秒後に本番画面へ自動で切り替える（視差効果を減らす設定の環境では 0.9 秒、アニメーションなし） |

### 3.3 本番画面

| ID | 要件 |
| --- | --- |
| B-1 | 上部の帯に「お題」ラベルとお題を大きく表示する |
| B-2 | 中央の領域に発言を並べる（4.2 参照）。発言がないときは「回答者の発言を入力すると、ここに並びます」と表示する |
| B-3 | 下部に入力バーを表示する。構成は、発言者タブ、発言の入力欄、「1つ戻す」「全画面」「新しいお題」ボタン、キー操作の説明 |
| B-4 | 本番画面に切り替わった時点で発言の入力欄にフォーカスする |

## 4. 機能要件

### 4.1 発言の入力

| ID | 要件 |
| --- | --- |
| F-1 | 入力欄で `Enter` を押すと、入力内容（前後の空白を除く）を現在の発言者の発言として末尾に追加し、入力欄を空にする |
| F-2 | 空文字の場合は追加しない |
| F-3 | IME 変換中の `Enter`（確定操作）では追加しない |
| F-4 | 追加後、発言者を次の回答者に進める。最後の回答者の次は最初の回答者に戻る |
| F-5 | `Tab` で発言者を次へ、`Shift+Tab` で前へ切り替える（循環する）。フォーカスは入力欄に留まる |
| F-6 | 発言者タブをクリックすると、その回答者を発言者にして入力欄にフォーカスする |
| F-7 | 入力欄の枠線の色とプレースホルダー「（名前）の発言をタイプして Enter」で、現在の発言者を示す |

### 4.2 発言の表示

| ID | 要件 |
| --- | --- |
| D-1 | 各発言を、通し番号（1 から）、発言内容、回答者名とともに表示する。番号・枠・名前は回答者の色で示す |
| D-2 | 並び順は上から下、列が埋まったら右隣の列の一番上へ続く（列優先） |
| D-3 | 1 列の行数は「回答者数 × 2」とする（例: 3 人なら 6 行、4 人なら 8 行） |
| D-4 | 列数は「発言数 ÷ 1 列の行数」の切り上げとし、最低 3 列とする。3 列に収まる間は列幅が変わらず、表示済みの発言の位置が動かない |
| D-5 | 最新の発言は背景を回答者の色にして強調する |
| D-6 | 新しく追加された発言は、短いポップアニメーションで表示する（視差効果を減らす設定の環境ではアニメーションなし） |
| D-7 | 発言内容は折り返さずに 1 行で表示する |
| D-8 | すべての発言が表示領域に収まるよう、文字サイズを自動調整する。上限は「画面高さの 14%」「画面幅の 9%」「150px」のうち最小の値、下限は 10px。発言の追加・削除、ウィンドウサイズの変更、全画面の切替、入力バーの表示切替のたびに再計算する |

### 4.3 取り消し

| ID | 要件 |
| --- | --- |
| U-1 | 「1つ戻す」ボタン、または入力欄が空の状態での `Backspace` で、最後の発言を削除する |
| U-2 | 取り消し後、発言者を削除した発言の回答者に戻す |
| U-3 | 発言がない場合は何もしない |

### 4.4 表示専用モード

| ID | 要件 |
| --- | --- |
| V-1 | 本番画面で `Esc` を押すと入力バーを隠し、もう一度押すと表示する |
| V-2 | 入力バーを隠している間に文字キー（修飾キーなし）を押すと、入力バーを再表示して入力欄にフォーカスする |

### 4.5 新しいお題

| ID | 要件 |
| --- | --- |
| R-1 | 発言がないときは、「新しいお題」を 1 回押すとお題入力画面に戻る |
| R-2 | 発言があるときは、1 回目の押下でボタンを強調色にし、表示を「もう一度押すと回答を消去」に変える。3 秒以内に再度押すと確定し、押さなければ元の表示に戻る |
| R-3 | 確定時は発言を消去し、お題欄を空にしてお題入力画面に戻る。入力バーが隠れていた場合は表示状態に戻す |
| R-4 | 回答者の人数と名前は次のラウンドに引き継ぐ |
| R-5 | ブラウザの確認ダイアログ（`confirm()` など）は使わない（埋め込み表示環境で動作しないため） |

### 4.6 全画面表示

| ID | 要件 |
| --- | --- |
| FS-1 | お題入力画面の「全画面」ボタンと、本番画面の入力バーの「全画面」ボタンで、ページ全体の全画面表示を切り替える |
| FS-2 | 全画面中は、両方のボタンの表示を「全画面を終了」にする |
| FS-3 | 全画面のまま画面遷移（お題入力 → 開始アナウンス → 本番、新しいお題で戻る）しても全画面を維持する |
| FS-4 | ボタン操作の後は、表示中の画面の入力欄（お題欄または発言欄）にフォーカスを戻す |

## 5. 表示デザイン

| ID | 要件 |
| --- | --- |
| UI-1 | 基調色はターメリック（背景 `#F2B63C`）、文字色は濃い茶（`#3A1F10`）、発言の地はココナッツミルク色（`#FFF6E2`） |
| UI-2 | 回答者の色は順に 唐辛子 `#C8321F`、バジル `#2F7A3E`、藍 `#2B4F9E`、紫玉ねぎ `#7B3F9E`、ミント `#0E7C7B`、ビーツ `#B0306A` |
| UI-3 | OS がダークモードのときは暗い配色に切り替える（背景 `#2A1A0E`、文字 `#FFE9BF`、回答者色は明るめの対応色） |
| UI-4 | フォントは Zen Maru Gothic（Google Fonts）。読み込めない場合はヒラギノ丸ゴ、ヒラギノ角ゴ、メイリオ、sans-serif の順で代替する |
| UI-5 | キーボード操作時のフォーカス位置を枠線で示す |

## 6. 非機能要件・制約

| ID | 要件 |
| --- | --- |
| N-1 | `index.html` 1 ファイルで完結する。ビルド工程、サーバー、外部ライブラリは使わない（Google Fonts の読み込みのみ例外） |
| N-2 | ローカルファイルとして直接開いても、GitHub Pages 上でも同じように動作する |
| N-3 | データは保存しない。再読み込みするとお題・発言・人数・名前は初期状態に戻る |
| N-4 | 公開は GitHub Pages（`main` ブランチ / ルート）で行う |

## 7. 既知の制約

- 全画面中に `Esc` を押すと、ブラウザの全画面解除が優先され、入力バーの表示切替と同時に全画面も解除されることがある。
- Claude のプレビューなど埋め込み表示の環境では、全画面ボタンが動作しない場合がある。

## 8. 受け入れ確認チェックリスト

変更を push する前に、該当する項目をブラウザで確認する。

- [ ] お題を空のまま `Enter` → 開始されず、メッセージが出る（S-7）
- [ ] 人数を 1 と 6 にすると、それぞれ − と ＋ が無効になる（S-3）
- [ ] 名前を変更 → 人数を減らす → 戻す で、名前が残っている（S-5）
- [ ] 日本語変換の確定 `Enter` で発言が追加されない（F-3）
- [ ] `Enter` のたびに発言者が人数分を循環する。`Tab`／`Shift+Tab` でも循環する（F-4, F-5）
- [ ] 3 人で 7 個目の発言が 2 列目の一番上に並ぶ。4 人なら 9 個目（D-2, D-3）
- [ ] 発言を増やし続けても全体が画面内に収まる（D-8）
- [ ] 空欄で `Backspace` → 最後の発言が消え、発言者がその人に戻る（U-1, U-2）
- [ ] `Esc` で入力バーが隠れ、文字を打つと戻る（V-1, V-2）
- [ ] 発言がある状態で「新しいお題」を 1 回押して 3 秒待つ → 元に戻る。2 回押す → お題入力画面に戻り、人数と名前が残っている（R-2〜R-4）
- [ ] お題入力画面で全画面にしてから開始 → 本番画面でも全画面のまま、ボタン表示が「全画面を終了」（FS-1〜FS-3）

## 9. 変更履歴

| バージョン | 内容 |
| --- | --- |
| 1.0 | 初版。ここまでの実装（お題入力、開始アナウンス、発言の列優先表示、番号・最新強調、自動文字サイズ、取り消し、表示専用モード、新しいお題の 2 回押し、全画面、回答者人数 1〜6 人、1 列の行数＝回答者数×2）を仕様化 |
