# massaman-board 仕様書

この文書は massaman-board の機能・画面・動作の「正」となる仕様です。
実装（`index.html`）を変更するときは、先にこの文書を更新し、受け入れ条件に沿って確認します。

- 文書バージョン: 4.3
- 対象実装: 夏祭りのデザインテーマに合わせた「夜の砂漠・諜報」デザインと、解読・判子・砂塵の演出を含む版

---

## 追加仕様（4.0：以下は旧記述に優先する）

- 初期画面は見出しを置かず、入力欄のプレースホルダーを「1人目の挑戦者」とする。Enter で確定すると入力欄を空にし、「2人目の挑戦者」「3人目の挑戦者」とプレースホルダーを更新して連続入力できる。中間画面・登録完了メッセージ・次へボタンは設けない。空欄は登録せず短い案内を表示する。IME の確定 Enter は除外する。
- 登録済みの参加者は名前入力画面内に番号・色付きで一覧表示する。専用の登録一覧画面には遷移しない。2名以上の登録で「お題の入力へ」ボタンを表示し、お題入力へ進める。0〜1名では非表示にする。取り消しで1名に戻った場合も非表示にする。入力中の未確定の名前があれば Enter での確定を案内し、遷移しない。ゲームの人数は登録数から自動決定（2〜6人）。6人で名前入力を無効にし「参加者が6名になりました」と表示する。最後の登録は「1つ戻す」で取り消せる。
- お題入力では登録者を表示し、「参加者を編集」で戻れる。新しいお題では登録を保持してお題入力へ戻る。
- 本番の各参加者ボックスに「脱落にする」ボタンを設ける。脱落者は文字をグレーアウトし、名前の横に `✕` アイコンを表示する。選択と自動切替・Tab の対象から外す。「復帰させる」で取り消せる。過去の発言は変更しない。発言の取り消しで脱落者の手番に戻る場合は次の参加中の回答者を選ぶ。
- 2人以上で開始し、脱落操作で残り1人になったら「勝者」と名前を大きく表示する。「ボードに戻る」で閉じ、ボックスにも勝者の印を残す。全員脱落は不可。1人参加では自動勝者表示をしない。
- 勝者表示は「勝者」→「挑戦者名 さん」→「おめでとう！」の順に表示し、「さん」は名前より小さくする。放射・勝者確定スタンプ・金色の砂塵の演出で決定を強調し、勝利画面の表示中は画面上部から金粉を継続して舞い落とす。放射の外周は画面内に見せず、視差効果を減らす設定ではアニメーションを停止する。
- 勝者決定後は発言追加を停止し、復帰で2人以上になれば再開する。新ラウンドでは脱落・勝者をリセットする。
- 同一ラウンドの記録済み発言と、前後の空白を除いた完全一致で重複判定する。表記揺れ、大小文字、全半角は区別する。重複時は中央に「すでに出た発言です」と該当発言を約2秒表示し、記録数・手番を変えない。入力は修正用に残して選択する。次の入力でも通知を消す。重複による自動脱落は行わない。
- 取り消した発言は再登録できる。全画面はエントリーから維持し、追加演出も視差効果を減らす設定に従う。
- 画面文言はゲーム進行に合わせた短い表現にする。エントリー画面は見出しを表示せず、入力欄のプレースホルダーを「N人目の挑戦者」とする。お題画面も見出しを表示せず、入力欄のプレースホルダーを「お題」とする。開始操作は「ゲーム開始」、参加者編集は「参戦者を替える」、次ラウンドは「次のお題」と表示する。
- 挑戦者名とお題の入力中も発言入力と同じ解読エコーを中央に表示し、ラベルはそれぞれ「エントリー」「お題」とする。入力を確定または空にするとエコーを消す。
- エントリー画面の補助説明や人数カウントは表示しない。登録済み参加者の番号・色付き一覧と、2名以上で表示する「お題へ」だけを表示する。
- 画面右上または入力ドックの太陽／月アイコンで、ダークモード（夜の砂漠）とライトモード（白背景・暗色文字）を切り替えられる。切替後も現在の画面と入力内容を保持する。
- 入力ドックは画面下部のフッターとして表示し、初期状態は折りたたむ。ドック上端に吸着した `⌃` / `⌄` アイコンで開閉する。挑戦者ストリップはドックの上に独立して表示し、常に表示する。

### 追加の受け入れ確認

- [ ] エントリーからお題入力へ進め、戻って名前・人数を編集できる。
- [ ] 脱落マーク・復帰が機能し、Enter / Tab / Shift+Tab は脱落者を飛ばす。
- [ ] 残り1人で正しい勝者が表示され、閉じても印が残る。復帰で入力再開できる。
- [ ] 勝者表示で「おめでとう！」と放射・スタンプ・砂塵の演出が表示され、放射の外周が画面内に見えない。
- [ ] 重複は通知だけで記録・手番が変わらず、修正後の発言は追加できる。
- [ ] 新しいお題では登録を保持し、新ラウンドで脱落・勝者・重複判定をリセットする。

## 1. 目的と利用場面

マッサマンゲームの進行中に、お題と回答者の発言をリアルタイムで大きく表示し、会場を盛り上げる。

- 利用者（操作者）: 司会・入力係。キーボードで発言を入力する
- 閲覧者: 会場の参加者・観客。プロジェクターや大型モニターで表示を見る
- 想定環境: PC のデスクトップブラウザ（Chrome / Edge / Safari / Firefox の最新版）。日本語入力（IME）を使う。照明を落とした会場での投影を主に想定する

## 2. 用語

| 用語 | 意味 |
| --- | --- |
| お題 | ラウンドごとに設定するテーマ文字列 |
| 回答者 | 発言する参加者。1ラウンドにつき1〜6人 |
| 発言 | 回答者が言った単語・フレーズ。入力係がタイプして追加する |
| 発言者 | 次に追加される発言の持ち主として選ばれている回答者 |
| ラウンド | お題の設定から「新しいお題」までの1回分 |
| エコー | 発言をタイプしている間、画面中央に大きく浮かび上がる入力中の文字 |

## 3. 画面構成と遷移

```
[お題入力画面] --Enter／開始する--> [開始アナウンス] --自動--> [本番画面]
      ^                                                      |
      +----------------------「新しいお題」--------------------+
```

### 3.1 参加者エントリー・お題入力画面

| ID | 要件 |
| --- | --- |
| S-1 | 参加者画面は見出しを表示せず、名前入力欄に「N人目の挑戦者」を表示する |
| S-2 | 名前入力欄を画面を開いた時点でフォーカスする。Enterで登録し、次のN+1人目へ進む（IME確定Enterは除く） |
| S-3 | 登録済み参加者を番号・色付きで表示し、2名以上で「お題へ」を表示する |
| S-4 | お題画面は見出しを表示せず、入力欄に「お題」を表示する |
| S-5 | お題欄でEnterまたは「ゲーム開始」を押すと開始する |
| S-6 | お題が空のまま開始しようとした場合は開始せず、お題欄にフォーカスして短い案内を表示する |
| S-7 | 参加者は2〜6名。名前入力中はエントリーの解読エコーを表示する |
| S-8 | お題入力中はお題の解読エコーを表示する |
| S-9 | 右上に「全画面」ボタンを表示する（4.6 参照） |

### 3.2 開始アナウンス

| ID | 要件 |
| --- | --- |
| A-1 | 「それでは開始します」を画面中央に大きく表示する。上下から映画の黒帯が入り、文字は1文字ずつ解読される（E-5） |
| A-2 | 1.9 秒後に本番画面へ自動で切り替える（視差効果を減らす設定の環境では 0.9 秒、演出なし） |

### 3.3 本番画面

| ID | 要件 |
| --- | --- |
| B-1 | 上部に「お題」の判子とお題を大きく表示する。本番画面に切り替わった時点でお題が解読される（E-6）。右端に記録中を示す赤い点滅、発言数、現在時刻を計器風に表示する |
| B-2 | 中央の領域に発言を並べる（4.2 参照）。発言がないときは「発言した順に回答を並べます」と表示する |
| B-3 | 下部に操作パネル（入力ドック）を表示する。構成は、発言者タブ、発言の入力欄、「1つ戻す」「全画面」「新しいお題」ボタン、キー操作の説明 |
| B-4 | 本番画面に切り替わった時点で発言の入力欄にフォーカスする |

## 4. 機能要件

### 4.1 発言の入力

| ID | 要件 |
| --- | --- |
| F-1 | 入力欄で `Enter` を押すと、入力内容（前後の空白を除く）を現在の発言者の発言として末尾に追加し、入力欄を空にする |
| F-2 | 空文字の場合は追加しない |
| F-3 | IME 変換中の `Enter`（確定操作）では追加しない |
| F-4 | 追加後、発言者を次の回答者に進める。最後の回答者の次は最初の回答者に戻る |
| F-5 | `Tab` で発言者を次へ、`Shift+Tab` で前へ切り替える（循環する）。フォーカスは入力欄に留まる |
| F-6 | 発言者タブをクリックすると、その回答者を発言者にして入力欄にフォーカスする |
| F-7 | 入力欄の枠線の色とプレースホルダー「（名前）の発言をタイプして Enter」で、現在の発言者を示す |

### 4.2 発言の表示

| ID | 要件 |
| --- | --- |
| D-1 | 各発言を、通し番号（2桁以上でゼロ埋め。例: 07）、発言内容、回答者名とともに、機密ファイルの見出しのようなカードで表示する。番号・左端の帯・名前は回答者の色で示す |
| D-2 | 並び順は上から下、列が埋まったら右隣の列の一番上へ続く（列優先） |
| D-3 | 1 列の行数は「回答者数 × 2」とする（例: 3 人なら 6 行、4 人なら 8 行） |
| D-4 | 列数は「発言数 ÷ 1 列の行数」の切り上げとし、最低 3 列とする。3 列に収まる間は列幅が変わらず、表示済みの発言の位置が動かない |
| D-5 | 最新の発言は、回答者の色で淡く塗り、光彩をつけて強調する |
| D-6 | 新しく追加された発言は、エコーの位置から自分の位置へ飛んで着地する（E-3） |
| D-7 | 発言内容は折り返さずに 1 行で表示する |
| D-8 | すべての発言が表示領域に収まるよう、文字サイズを自動調整する。上限は「画面高さの 13%」「画面幅の 8.5%」「140px」のうち最小の値、下限は 10px。発言の追加・削除、ウィンドウサイズの変更、全画面の切替、入力ドックの表示切替のたびに再計算する |

### 4.3 取り消し

| ID | 要件 |
| --- | --- |
| U-1 | 「1つ戻す」ボタン、または入力欄が空の状態での `Backspace` で、最後の発言を削除する |
| U-2 | 取り消し後、発言者を削除した発言の回答者に戻す |
| U-3 | 発言がない場合は何もしない |

### 4.4 表示専用モード

| ID | 要件 |
| --- | --- |
| V-1 | 本番画面の挑戦者ストリップにある `⌃` / `⌄` アイコンを押すと入力ドックを折りたたみ／表示する。`Esc` はブラウザの全画面操作に使用する |
| V-2 | 入力ドックを隠している間に文字キー（修飾キーなし）を押すと、入力ドックを再表示して入力欄にフォーカスする |

### 4.5 新しいお題

| ID | 要件 |
| --- | --- |
| R-1 | 発言がないときは、「新しいお題」を 1 回押すとお題入力画面に戻る |
| R-2 | 発言があるときは、1 回目の押下でボタンを強調色にし、表示を「もう一度押すと回答を消去」に変える。3 秒以内に再度押すと確定し、押さなければ元の表示に戻る |
| R-3 | 確定時は発言を消去し、お題欄を空にしてお題入力画面に戻る。入力ドックが隠れていた場合は表示状態に戻す。エコーも消す |
| R-4 | 回答者の人数と名前は次のラウンドに引き継ぐ |
| R-5 | ブラウザの確認ダイアログ（`confirm()` など）は使わない（埋め込み表示環境で動作しないため） |

### 4.6 全画面表示

| ID | 要件 |
| --- | --- |
| FS-1 | お題入力画面の「全画面」ボタンと、本番画面の入力ドックの「全画面」ボタンで、ページ全体の全画面表示を切り替える |
| FS-2 | 全画面中は、両方のボタンの表示を「全画面を終了」にする |
| FS-3 | 全画面のまま画面遷移（お題入力 → 開始アナウンス → 本番、新しいお題で戻る）しても全画面を維持する |
| FS-4 | ボタン操作の後は、表示中の画面の入力欄（お題欄または発言欄）にフォーカスを戻す |

## 5. 演出（視覚効果）

| ID | 要件 |
| --- | --- |
| E-1 | エコー: 発言の入力欄に文字があるあいだ、入力中の文字列を画面中央に巨大な文字で表示する。上に赤い点滅と「受信中」、発言者名を添え、文字には回答者の色の光彩をつける。日本語変換中の未確定文字も表示する |
| E-2 | 解読: エコーで新たに増えた文字（変換で置き換わった文字を含む）は、約 0.3 秒のあいだランダムな文字でちらついてから確定し、確定の瞬間に光る。長い文字列は画面幅の 92% に収まるよう縮小する |
| E-3 | エコー表示中は、背後の発言一覧を暗くぼかして、エコーを主役にする。入力欄が空になるとエコーを消して一覧を元に戻す |
| E-4 | 着地: `Enter` で発言を追加すると、エコーの位置・大きさから一覧上の位置へ、発言が縮みながら飛んで着地する。着地時は判子を押すような衝撃と波紋を出し、砂色の砂塵が風下（右方向）へ舞う |
| E-5 | 開始アナウンスでは、上下から黒帯が入り、文字が1文字ずつ時間差で解読される。表示の途中で画面を横切る砂塵が舞う |
| E-6 | 本番画面に切り替わると、お題が先頭から順に解読されて現れ、「お題」の判子が押される |
| E-7 | 視差効果を減らす設定（`prefers-reduced-motion: reduce`）の環境では、解読・飛行・判子・黒帯・点滅の動きと砂塵を行わない。文字は最初から確定状態で表示し、エコーの表示（E-1, E-3）は動きなしで行う |
| E-8 | 演出の最中も入力を受け付け、演出の完了を待たずに次の発言を追加できる |

## 6. 表示デザイン

| ID | 要件 |
| --- | --- |
| UI-1 | 夏祭りのデザインテーマ（ドラマ『VIVANT』の世界観）に合わせ、コンセプトを「夜の砂漠と諜報の機密ファイル」とする。番組のロゴ・題字・キービジュアルは再現せず、色・質感・空気感のみを取り入れる |
| UI-2 | 背景は夜空の暗色（`#100D0A`）から、画面下の地平線に向けて砂色にかすかに光るグラデーションとする。地図の方眼とフィルムの粒子をうっすら重ねる。配色は OS のライト／ダーク設定によらず固定する |
| UI-3 | 文字色は砂の白（`#EDE3CF`）、補助文字は同色の半透明。基調のアクセントは砂色（`#C9A56A`）、判子・記録中・確定操作は信号の赤（`#D23B2A`） |
| UI-4 | 回答者の色は順に 赤砂 `#E0533B`、砂金 `#D9A441`、オアシス `#3FA595`、薄暮の青 `#6F96CC`、草原 `#9DB46B`、夜明けの紫 `#A881BE` |
| UI-5 | 見出し・お題・発言・エコーは重い明朝体（Zen Old Mincho）、操作部分や補助文字は Zen Kaku Gothic New、番号・時刻など計器風の表示は等幅フォント（Share Tech Mono）。いずれも Google Fonts。読み込めない場合は明朝系・ゴシック系・等幅の標準フォントで代替する |
| UI-6 | 入力ドックは画面下部に浮かぶ半透明のパネルとし、背景をぼかす |
| UI-7 | キーボード操作時のフォーカス位置を砂色の枠線で示す |

## 7. 非機能要件・制約

| ID | 要件 |
| --- | --- |
| N-1 | `index.html` 1 ファイルで完結する。ビルド工程、サーバー、外部ライブラリは使わない（Google Fonts の読み込みのみ例外）。パーティクルは Canvas、飛行アニメーションは Web Animations API で実装する |
| N-2 | ローカルファイルとして直接開いても、GitHub Pages 上でも同じように動作する |
| N-3 | データは保存しない。再読み込みするとお題・発言・人数・名前は初期状態に戻る |
| N-4 | 公開は GitHub Pages（`main` ブランチ / ルート）で行う |
| N-5 | 演出はタイピングの反応を遅らせない。パーティクルは存在するときだけ描画ループを回す |

## 8. 既知の制約

- 全画面中に `Esc` を押すと、ブラウザの全画面解除が優先され、入力ドックの表示切替と同時に全画面も解除されることがある。
- Claude のプレビューなど埋め込み表示の環境では、全画面ボタンが動作しない場合がある。
- `color-mix()` に対応していない古いブラウザでは、発言の枠や光彩の色合いが簡略化される。

## 9. 受け入れ確認チェックリスト

変更を push する前に、該当する項目をブラウザで確認する。

- [ ] お題を空のまま `Enter` → 開始されず、メッセージが出る（S-7）
- [ ] 「開始する」ボタンでも開始できる（S-6）
- [ ] 人数を 1 と 6 にすると、それぞれ − と ＋ が無効になる（S-3）
- [ ] 名前を変更 → 人数を減らす → 戻す で、名前が残っている（S-5）
- [ ] 開始時に黒帯が入り、文字が解読され、砂塵が舞う。本番画面でお題が解読されて現れ、判子が押される（E-5, E-6）
- [ ] 発言をタイプすると、中央に巨大な文字が解読されながら現れ、背後の一覧が暗くなる。変換中の文字も出る（E-1〜E-3）
- [ ] 日本語変換の確定 `Enter` で発言が追加されない（F-3）
- [ ] `Enter` で発言が中央から一覧の位置へ飛んで判子のように着地し、波紋と砂塵が出る（E-4）
- [ ] 続けて素早く入力・追加しても取りこぼさない（E-8）
- [ ] `Enter` のたびに発言者が人数分を循環する。`Tab`／`Shift+Tab` でも循環し、エコーの色と名前も切り替わる（F-4, F-5, E-1）
- [ ] 3 人で 7 個目の発言が 2 列目の一番上に並ぶ。4 人なら 9 個目（D-2, D-3）
- [ ] 発言を増やし続けても全体が画面内に収まる（D-8）
- [ ] 空欄で `Backspace` → 最後の発言が消え、発言者がその人に戻る（U-1, U-2）
- [ ] `⌃` / `⌄` で入力ドックを折りたたみ、文字入力で自動再表示する（V-1, V-2）
- [ ] 発言がある状態で「新しいお題」を 1 回押して 3 秒待つ → 元に戻る。2 回押す → お題入力画面に戻り、人数と名前が残っている（R-2〜R-4）
- [ ] お題入力画面で全画面にしてから開始 → 本番画面でも全画面のまま、ボタン表示が「全画面を終了」（FS-1〜FS-3）
- [ ] OS の「視差効果を減らす」を有効にすると、動きと砂塵がなくなり、文字は最初から確定状態で出る（E-7）

## 10. 変更履歴

| バージョン | 内容 |
| --- | --- |
| 1.0 | 初版。ここまでの実装（お題入力、開始アナウンス、発言の列優先表示、番号・最新強調、自動文字サイズ、取り消し、表示専用モード、新しいお題の 2 回押し、全画面、回答者人数 1〜6 人、1 列の行数＝回答者数×2）を仕様化 |
| 2.0 | デザインを「ステージ」コンセプトに刷新（UI-1〜UI-6）。演出を追加（E-1〜E-8: エコー、着地、開始時・お題表示時のパーティクル）。「開始する」ボタンを追加（S-6）。ダークモード切替を廃止し配色を固定 |
| 3.0 | 夏祭りのデザインテーマ（ドラマ『VIVANT』の世界観）に合わせ、「夜の砂漠と諜報の機密ファイル」に刷新（UI-1〜UI-7）。演出を解読・判子・砂塵・黒帯に変更（E-1〜E-7）。お題欄に記録中・発言数・時刻を追加（B-1）。番号をゼロ埋めに（D-1） |
| 4.3 | 勝者表示を「おめでとう！」と放射・勝者確定スタンプ・砂塵の演出で強化。全画面時に放射の外周が見えないよう調整 |
